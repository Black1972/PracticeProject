name: Deploy Project
on: [push, workflow_dispatch]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: testing
        run: echo "testing..."
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: deploying
        run: echo "Deploying"