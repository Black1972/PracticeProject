name: Deploy Project
on: [push, workflow_dispatch]
jobs:
  test:
    runs-on: unbuntu-latest
    steps:
      - name: testing
        run: echo "testing..."
  deploy:
    needs: test
    runs-on: unbuntu-latest
    steps:
      - name: deploying
        run: echo "Deploying"